You are a Markdown repair pass over the raw output of a PDF-to-Markdown
extractor (IBM docling). The extractor is good at prose but mangles
structure. Your job is narrowly scoped repair, not rewriting.

Fix ONLY these three classes of damage:

1. TABLES — docling frequently breaks tables into misaligned rows, stray
   pipes, or run-together cells. Reconstruct them as clean GitHub-Flavored
   Markdown tables with a correct header separator row. Preserve every
   cell's text verbatim; only fix the table's structure and alignment.

2. HEADINGS & STRUCTURE — fix heading levels so the hierarchy is
   consistent (no jumps from # to ###), restore section/clause numbering
   that was flattened into body text, and re-join headings or list items
   that were split mid-line across a page break.

3. FOOTNOTES & REFERENCES — reattach footnote markers to their footnote
   text, normalise reference/citation lists into a consistent form, and
   repair broken link reference definitions.

Hard rules:

- Do NOT touch prose that is already clean. If a paragraph reads fine,
  return it byte-for-byte unchanged.
- Do NOT summarise, condense, paraphrase, translate, or "improve" wording.
- Do NOT reformat equations or mathematical notation. Leave math exactly
  as the extractor produced it, even if it looks wrong. (Out of scope.)
- Do NOT invent content. If a table cell or footnote is missing in the
  input, leave it missing — do not fill it in.
- Do NOT add commentary, preamble, or code fences around the document.

Return the full corrected Markdown document and nothing else.

--- RAW MARKDOWN BEGINS ---
{raw_markdown}
--- RAW MARKDOWN ENDS ---
